{% extends 'base.html.twig' %}

{% block body %}
<div class="container my-5 d-flex justify-content-center">
<div class="card shadow-lg border-0 rounded-4 p-4 bg-dark text-white w-50">
    <h1 class="text-center text-warning">🔑 Connexion</h1>

    {% if error %}
        <div class="alert alert-danger text-center rounded-3 mt-3">
            {{ error.messageKey|trans(error.messageData, 'security') }}
        </div>
    {% endif %}

    <form action="{{ path('app_login') }}" method="post">
        <div class="mb-3">
            <label for="username" class="form-label fw-bold">📧 Votre email :</label>
            <input class="form-control bg-secondary text-white border-0 rounded-3 p-2" 
                type="text" placeholder="exemple@mail.com" id="username" name="_username" 
                value="{{ last_username }}" required>
        </div>

        <div class="mb-3">
            <label for="password" class="form-label fw-bold">🔒 Votre mot de passe :</label>
            <input class="form-control bg-secondary text-white border-0 rounded-3 p-2" 
                type="password" placeholder="••••••••" id="password" name="_password" required>
        </div>

        <input type="hidden" name="_target_path" value="{{ path('app_account') }}">

        <button type="submit" class="btn btn-warning w-100 fw-bold py-2">🚀 Se connecter</button>
    </form>

    <div class="mt-3 d-flex justify-content-between">
        <div>
            <p class="d-inline">Vous n'avez pas de compte ?</p>
            <a class="d-inline text-warning fw-bold" href="{{ path('app_register') }}">S'inscrire</a>
        </div>
        <a class="text-warning fw-bold" href="{{ path('app_password') }}">Mot de passe oublié ?</a>
    </div>
</div>
</div>

{% endblock %}
